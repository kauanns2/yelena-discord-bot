id: character.appearance

name: Appearance

display_name: Character Appearance

description: >
  Subsistema responsável por definir todas as características físicas,
  visuais e estéticas da personagem. Fornece uma representação
  consistente da aparência para os demais módulos do sistema.

type: subsystem

module: character

version: 1.0.0

status: stable

enabled: true

priority: 1.9

persistent: true

entrypoint: ../README.md

system:

  registry: ../system/registry.yaml

  state: ../system/state.yaml

  links: ../system/links.yaml

definition:

  metadata: metadata.yaml

  schema: schema.yaml

  dependencies: dependencies.yaml

  version: version.yaml

exports:

  - appearance

  - visual_profile

  - physical_traits

tags:

  - appearance
  - character
  - visual
  - physical
  - core
