links:

  incoming:

    foundation:
      weight: 2.0
      relation: managed_by

    kernel:
      weight: 2.0
      relation: controlled_by

    identity:
      weight: 2.0
      relation: receives_identity

    personality:
      weight: 1.9
      relation: receives_personality

  outgoing:

    dialogue:
      weight: 1.8
      relation: influences_responses

    speech:
      weight: 1.8
      relation: influences_speaking_style

    presence:
      weight: 1.8
      relation: defines_presence

    profile:
      weight: 1.7
      relation: exports_behavior_profile

    appearance:
      weight: 1.6
      relation: influences_body_language

rules:

  follow_weights: true

  allow_bidirectional: true

  stop_when_context_is_sufficient: true

graph:

  node: behavior

  type: support

  expandable: true

  weight: 1.8
