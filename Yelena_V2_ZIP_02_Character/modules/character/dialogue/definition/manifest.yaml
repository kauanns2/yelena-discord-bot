id: character.dialogue

name: Dialogue

display_name: Character Dialogue

description: >
  Subsistema responsável por estruturar diálogos, manter contexto,
  organizar respostas e garantir a continuidade das conversas da
  personagem.

type: subsystem

module: character

version: 1.0.0

status: stable

enabled: true

priority: 1.7

persistent: true

entrypoint: ../README.md

system:

  registry: ../system/registry.yaml

  state: ../system/state.yaml

  links: ../system/links.yaml

definition:

  metadata: metadata.yaml

  schema: schema.yaml

  dependencies: dependencies.yaml

  version: version.yaml

exports:

  - dialogue

  - conversation_flow

  - context

  - responses

tags:

  - dialogue
  - communication
  - conversation
  - context
  - web-brain
