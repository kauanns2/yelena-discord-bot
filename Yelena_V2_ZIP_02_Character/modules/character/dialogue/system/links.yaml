links:

  incoming:

    foundation:
      weight: 2.0
      relation: managed_by

    kernel:
      weight: 2.0
      relation: controlled_by

    identity:
      weight: 2.0
      relation: receives_identity

    personality:
      weight: 1.9
      relation: receives_personality

    behavior:
      weight: 1.8
      relation: receives_behavior

    speech:
      weight: 1.8
      relation: receives_speech

  outgoing:

    presence:
      weight: 1.7
      relation: supports_presence

    profile:
      weight: 1.6
      relation: exports_dialogue_profile

    voice_style:
      weight: 1.6
      relation: coordinates_voice_style

rules:

  follow_weights: true

  allow_bidirectional: true

  stop_when_context_is_sufficient: true

graph:

  node: dialogue

  type: communication

  expandable: true

  weight: 1.7
