id: character.identity

name: Identity

display_name: Character Identity

description: >
  Define a identidade permanente da personagem. Este módulo reúne
  todas as informações que determinam quem a Yelena é e fornece
  essa identidade como referência para os demais módulos do sistema.

type: subsystem

module: character

version: 1.0.0

status: stable

enabled: true

priority: 2.0

persistent: true

entrypoint: ../README.md

system:

  registry: ../system/registry.yaml

  state: ../system/state.yaml

  links: ../system/links.yaml

definition:

  metadata: metadata.yaml

  schema: schema.yaml

  dependencies: dependencies.yaml

  version: version.yaml

exports:

  - identity

  - profile

  - reference

tags:

  - identity
  - character
  - core
  - profile
  - persistent
