id: character.personality

name: Personality

display_name: Character Personality

description: >
  Subsistema responsável por definir os traços psicológicos,
  valores, crenças, preferências e padrões cognitivos da personagem.

type: subsystem

module: character

version: 1.0.0

status: stable

enabled: true

priority: 1.9

persistent: true

entrypoint: ../README.md

system:

  registry: ../system/registry.yaml

  state: ../system/state.yaml

  links: ../system/links.yaml

definition:

  metadata: metadata.yaml

  schema: schema.yaml

  dependencies: dependencies.yaml

  version: version.yaml

exports:

  - personality

  - traits

  - values

  - beliefs

  - preferences

tags:

  - personality
  - psychology
  - character
  - cognition
  - web-brain
